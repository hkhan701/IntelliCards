CREATE SCHEMA PUBLIC AUTHORIZATION DBA
CREATE MEMORY TABLE PUBLIC.USERS(USERNAME VARCHAR(255) NOT NULL PRIMARY KEY,PASSWORD VARCHAR(255) NOT NULL)
CREATE MEMORY TABLE PUBLIC.FLASHCARDS(CARDID VARCHAR(255) NOT NULL, SETID VARCHAR(255) NOT NULL, QUESTION VARCHAR(255) NOT NULL, ANSWER VARCHAR(255) NOT NULL, HINT VARCHAR(255), DELETED BOOLEAN DEFAULT FALSE NOT NULL, ATTEMPTS INTEGER DEFAULT 0 NOT NULL, CORRECT INTEGER DEFAULT 0 NOT NULL, CONSTRAINT CONSTRAINT1 PRIMARY KEY(CARDID,SETID),CONSTRAINT CONSTRAINT2 FOREIGN KEY(SETID) REFERENCES FLASHCARDSETS(SETID))
CREATE MEMORY TABLE PUBLIC.FLASHCARDSETS(SETID VARCHAR(255) NOT NULL, USERNAME VARCHAR(255) NOT NULL, SETNAME VARCHAR(255) NOT NULL, CONSTRAINT CONSTRAINT1 PRIMARY KEY(SETID,USERNAME),CONSTRAINT CONSTRAINT2 FOREIGN KEY(USERNAME) REFERENCES USERS(USERNAME))
CREATE USER SA PASSWORD DIGEST 'd41d8cd98f00b204e9800998ecf8427e'
ALTER USER SA SET LOCAL TRUE
CREATE SCHEMA PUBLIC AUTHORIZATION DBA
SET DATABASE DEFAULT INITIAL SCHEMA PUBLIC
GRANT DBA TO SA
SET WRITE_DELAY 20
SET FILES SCALE 32
SET SCHEMA PUBLIC
INSERT INTO USERS('user1', 'password1')
INSERT INTO USERS('user2', 'password2')
INSERT INTO FLASHCARDSETS(0, 'user1', 'set1')
INSERT INTO FLASHCARDSETS(1, 'user1', 'set2')
INSERT INTO FLASHCARDS(0, 0, 'q1', 'a1', 'h1', false, 0, 0)
INSERT INTO FLASHCARDS(0, 1, 'q2', 'a2', null, false, 0, 0)


